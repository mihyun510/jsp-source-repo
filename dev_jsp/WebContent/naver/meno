캡차 API 사용하기
ajax을 사용하면 캡쳐이미지가 바뀌었으면 좋겠다.
로그인을 하면 사용가능?
왼쪽에 이미지를 띄우고 오른쪽에 새로고침 버튼을 놓는다
밑에는 textbox을 하나 놓는다. 그 옆에는 검사버튼을 놓는다.
아작스를 사용하여 구현
$.ajax({
  method:"get" //get으로 해본 후 post방식으로 해보자
  ,url:
 });
  X-Naver-Client-Id: {애플리케이션 등록 시 발급받은 클라이언트 아이디 값}
  X-Naver-Client-Secret: {애플리케이션 등록 시 발급받은 클라이언트 시크릿 값}
- 꼭 필요한 것들

https://openapi.naver.com/v1/captcha/ncaptcha.bin -> 요청URL
-> bin을 붙힌 이유 무엇임? 검색창에 네이버 개발자센터를 검색하면 search.naver? 이런 url을 확인 가능함 보면 search가 jsp와 서블릿을 구별하는 이름이고 .naver은 .do와 같은 역할 
-> bin은 .naver와 같은 의미 아무의미없다. 

톰캣으로 개발.. jsp과 servlet페이지 제공

-> 톰캣의 표준서블릿이 처리하는 페이지는 jsp이고 servlet이 중간에 인터셉트하는 것은 .do일때 한다.
-> 톰캣의 표준서블릿이 처리하는 페이지는 httpServlet프로토콜이 인터셉트하는 것이다.

API를 처리하는 주체 : 브라우저
서버쪽에 가상의 머신을 설치해서 사용 - 안드로이드 사용.,..,

요청하면 응답으로 캡차이미지를 반환한다.
이미지를 받아서 보여주고 입력을 받는다
value값을 받는다
결과값을 처리하는 값이 ..? 요청을 할 댸는 값을 1로 설정
value 사용자가 입력한 값 
